/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here.
  // For now, an empty configuration is sufficient to resolve the build issue.
};

module.exports = nextConfig;
