{
  "version": 2,
  "buildCommand": "pnpm --filter api exec prisma generate && pnpm --filter web run build",
  "outputDirectory": "packages/web/.next",
  "installCommand": "pnpm install",
  "functions": {
    "packages/web/**/*.js": {
      "maxDuration": 30
    }
  }
}