{
  "version": 2,
  "buildCommand": "cd packages/api && npx prisma generate && cd ../web && npm run build",
  "outputDirectory": "packages/web/.next",
  "installCommand": "npm install -g pnpm && pnpm install",
  "functions": {
    "packages/web/**/*.js": {
      "maxDuration": 30
    }
  }
}