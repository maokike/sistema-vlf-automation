{
  "version": 2,
  "buildCommand": "npm run prisma:generate && npm run build --workspace=api && npm run build --workspace=web",
  "outputDirectory": "packages/web/.next",
  "installCommand": "npm install"
}